module.exports = {
    name: 'presenceUpdate',
    async execute(oldUser, newUser) {
        console.log('putting something here for later')
        // if (newUser.userId !== '269465677251870720') return;
        // if (!newUser.activities && !oldUser.activities) return
        // if (newUser.activities[0]?.name === oldUser.activities[0]?.name) return
        // if (newUser.guild.id !== '493462454316171290') return
        // const me = await newUser.guild.members.fetch('89973782285910016')
        // const activity = newUser.activities[0]?.name
        // const oldActivity = oldUser.activities[0]?.name
        // activity ? me.send(`maddie is now playing ${activity}`) : me.send(`maddie has stopped playing ${oldActivity}`)
    },
}